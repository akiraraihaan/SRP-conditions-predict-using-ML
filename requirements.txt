# Pinned to the versions actually installed in OLD_DIR/.venv at migration time
# (MIGRATION_NOTES.md section 9). The runtime versions are re-read and recorded
# into every registry record, so a drift is visible in the results, not silent.

# --- core ---
ultralytics==8.4.53
torch==2.12.0
torchvision==0.27.0

# --- numerics / data ---
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.8.0
scipy==1.17.1

# --- imaging ---
pillow==12.2.0
opencv-python==4.13.0.92

# --- plotting (matplotlib only; seaborn is deliberately NOT a dependency) ---
matplotlib==3.10.9

# --- profiling / utility ---
ultralytics-thop==2.0.19
PyYAML==6.0.3
tqdm==4.67.3
psutil==7.2.2

# --- statistics (Friedman / Nemenyi across folds) ---
scikit-posthocs==0.13.0
statsmodels==0.14.6

# NOTE on torch: OLD_DIR pinned the CPU build. On Kaggle the CUDA build of the
# same version is already present in the base image, so let the pre-installed
# wheel win rather than forcing a reinstall:
#     pip install -r requirements.txt --no-deps --upgrade-strategy only-if-needed
# See docs/KAGGLE_SETUP.md.
#
# NOTE for scripts/07_bench_edge.py on a Raspberry Pi: that script needs only
# torch (CPU), torchvision, pillow, numpy, psutil and PyYAML. It never imports
# ultralytics training code and never touches CUDA.
